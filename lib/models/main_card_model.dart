class News{
  final String title;
  final String miniInformation;
  final String photoPath;
  final String fullInformation;
  final String inviteTitle;

  const News(this.title, this.miniInformation, this.inviteTitle, this.fullInformation, this.photoPath);
}